---
subject: bash
snippets:
  - syntax: bash
    content: |
      case $var in
        one) echo "hello" ;;
        two) echo "world" ;;
      esac
---

# Case statement

```bash
case $var in
  one) echo "hello" ;;
  two) echo "world" ;;
esac
```
